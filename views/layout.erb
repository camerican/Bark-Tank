<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Bark Tank</title>
	<link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
	<link rel="stylesheet" type="text/css" href="/css/default.css" />
	<% if @current_user %>
	<style type="text/css">
	#login.loggedin {
		background-image: url(<%= @current_user.photo %>);
		background-size: 120%;
		background-position: -20% 35%;
		clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 0%);

	}
	</style>
	<% end %>
</head>
<body class="<%= klass if klass %>">
	<div id="login"<%= ' class="loggedin"' if @current_user %>>
		<% unless @current_user %>
		<div class="text"><a href="/auth/slack">Login</a></div>
		<% else %>
		<div class="text"><a href="/logout">Logout</a></div>
		<% end %>
	</div>
	<%= yield %>
	<script>
	  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
	  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
	  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
	  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
	  ga('create', 'UA-93545568-1', 'auto');
	  ga('send', 'pageview');
	</script>
</body>
</html>